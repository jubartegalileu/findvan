---
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, ux_consistency]
---

# Story 24.2 — Aba SDR: Mesa de Trabalho do Vendedor

**Epic:** 24 (SDR & Funil de Vendas: Workflow Linear)
**Story ID:** 24.2
**Status:** Done
**Priority:** P0
**Complexity:** Média (5 pontos)
**Blocked by:** Story 24.1

## Story

**As a** SDR/vendedor de transporte escolar,
**I want** a dedicated SDR tab with my daily work queue, quick actions, and performance counter,
**so that** I can see what I need to do today and execute my cadence without switching between tabs.

## Acceptance Criteria

1. Navigation
- [ ] Aba "SDR" visível no sidebar (Layout.jsx) entre "Leads" e "Funil"
- [ ] Rota `/sdr` registrada no App.jsx
- [ ] Ícone consistente com o design system existente

2. Fila do Dia
- [ ] Lista de leads ordenada: cadência vencida > cadência hoje > planejada, desempate por score (maior primeiro)
- [ ] Cada card exibe sem abrir modal: nome, empresa (company_name), telefone, score badge, último contato (last_contact_at formatado), próxima ação (next_action_description)
- [ ] Cards com cadência vencida têm indicador visual (borda vermelha ou badge)
- [ ] Cards com cadência hoje têm indicador visual (borda amarela ou badge)
- [ ] Lista atualiza ao executar ações (sem reload de página)

3. Quick Actions
- [ ] Botão WhatsApp: abre conversa via integração existente (mesmo comportamento da aba WhatsApp)
- [ ] Botão Telefone: click-to-call (`tel:` link com número formatado)
- [ ] Botão Anotar: campo inline que aparece no card, persiste via `PATCH /api/sdr/:leadId/notes`
- [ ] Botão "Feito": marca ação como concluída via `PATCH /api/sdr/:leadId/action`, card sai da fila ou atualiza estado

4. Filtros
- [ ] Filtro por status de cadência: Vencida, Hoje, Planejada (multi-select)
- [ ] Filtro por score range: slider ou input min/max
- [ ] Filtro por cidade: dropdown populado das cidades dos leads na fila

5. Performance Counter
- [ ] Barra no topo exibindo: "X contatos feitos hoje / Y pendentes"
- [ ] Valores atualizados em tempo real ao executar ações
- [ ] Dados consumidos de `GET /api/sdr/stats`

6. Quality
- [ ] Testes unitários para componentes SDR (card, fila, filtros)
- [ ] `npm run lint` passa
- [ ] `npm run typecheck` passa

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend
**Secondary Type(s)**: API Integration
**Complexity**: Média

### Specialized Agent Assignment

**Primary Agents**:
- @dev: Component implementation, API integration

**Supporting Agents**:
- @architect: Pattern validation, consistency with existing tabs

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Component structure, API integration review
- [ ] Pre-PR (@architect): UX consistency with Leads/Dashboard, performance check

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: [CRITICAL, HIGH]

### CodeRabbit Focus Areas

**Primary Focus**:
- API integration: Proper error handling on all fetch calls
- State management: Optimistic updates after quick actions
- Component patterns: Follow existing page component structure (Leads.jsx pattern)

**Secondary Focus**:
- Accessibility: Keyboard navigation for quick actions
- Performance: No unnecessary re-renders on action execution

## Tasks / Subtasks

- [ ] **Task 1: Page Component Setup** (AC: 1)
  - [ ] Criar `packages/dashboard/src/pages/SDR.jsx`
  - [ ] Adicionar rota `/sdr` no App.jsx
  - [ ] Adicionar item "SDR" no sidebar em Layout.jsx (entre Leads e Funil)
  - [ ] Escolher ícone consistente (sugestão: UserCheck ou Headphones do Lucide)

- [ ] **Task 2: Fila do Dia** (AC: 2)
  - [ ] Implementar fetch de `GET /api/sdr/queue` com estado de loading
  - [ ] Criar componente `SDRCard` com: nome, empresa, telefone, score (reusar ScoreBadge), último contato, próxima ação
  - [ ] Implementar indicadores visuais de cadência (vencida=vermelho, hoje=amarelo, planejada=verde)
  - [ ] Implementar ordenação client-side como fallback (se API já retorna ordenado, manter)

- [ ] **Task 3: Quick Actions** (AC: 3)
  - [ ] Botão WhatsApp: reusar lógica existente de WhatsApp.jsx para abrir conversa
  - [ ] Botão Telefone: `<a href="tel:+55${phone}">` com número formatado
  - [ ] Botão Anotar: toggle de campo textarea inline, submit via `PATCH /api/sdr/:leadId/notes`
  - [ ] Botão "Feito": chamada `PATCH /api/sdr/:leadId/action`, remover card da lista ou atualizar estado
  - [ ] Feedback visual após cada ação (toast ou animação de check)

- [ ] **Task 4: Filtros** (AC: 4)
  - [ ] Criar barra de filtros no topo (padrão similar ao FilterBar de Leads.jsx)
  - [ ] Filtro cadência: chips/buttons multi-select (Vencida, Hoje, Planejada)
  - [ ] Filtro score: input range ou min/max
  - [ ] Filtro cidade: dropdown com cidades únicas da fila
  - [ ] Aplicar filtros via query params na API ou client-side

- [ ] **Task 5: Performance Counter** (AC: 5)
  - [ ] Criar componente `SDRPerformanceBar` no topo da página
  - [ ] Fetch de `GET /api/sdr/stats` no mount e após cada ação
  - [ ] Exibir: "X contatos feitos hoje / Y pendentes" com ícones

- [ ] **Task 6: Tests** (AC: 6)
  - [ ] Testes unitários para SDRCard
  - [ ] Testes unitários para filtros
  - [ ] Verificar lint e typecheck

## Dev Notes

### Relevant Source Tree

```
packages/dashboard/src/
├── App.jsx                    # Adicionar rota /sdr
├── components/
│   ├── Layout.jsx             # Adicionar item SDR no sidebar
│   ├── ScoreBadge.jsx         # REUSAR para score nos cards
│   └── FilterBar.jsx          # REFERÊNCIA para padrão de filtros
├── pages/
│   ├── Leads.jsx              # REFERÊNCIA para padrão de página (fetch, filtros, estado)
│   ├── WhatsApp.jsx           # REFERÊNCIA para integração WhatsApp
│   ├── SDR.jsx                # CRIAR
│   └── Funnel.jsx             # Criada na Story 24.3
```

### API Endpoints (de Story 24.1)

```
GET    /api/sdr/queue?city=&prospect_status=&cadence=overdue|today|planned
PATCH  /api/sdr/:leadId/action   → body: { action_type, notes? }
PATCH  /api/sdr/:leadId/notes    → body: { note: "texto" }
GET    /api/sdr/stats            → { total, done_today, pending, overdue }
```

### API Base Pattern (de Leads.jsx)

```javascript
const API_BASE = (() => {
  const hostname = window.location.hostname;
  if (hostname === 'localhost' || hostname === '127.0.0.1') {
    const port = window.location.port;
    if (port === '5173' || port === '5174') return 'http://localhost:8000';
    return `http://${hostname}:8000`;
  }
  return '';
})();
```

### Sidebar Pattern (de Layout.jsx)

Seguir exatamente o padrão de items existentes no sidebar. Inserir SDR após Leads e antes do que será Funil.

### Testing

- **Framework:** Verificar se dashboard usa vitest ou outro (checar package.json)
- **Localização:** `packages/dashboard/src/__tests__/` ou `packages/dashboard/tests/`
- **Foco:** Renderização de componentes, chamadas de API mockadas

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-01 | 1.0 | Story criada a partir do Epic 24 | @sm (River) |

## Dev Agent Record

### Agent Model Used
GPT-5 Codex (CLI)

### Debug Log References
- Implementacao da aba SDR em `packages/dashboard/src/pages/SDR.jsx`
- Validacao dashboard: `npm run lint`, `npm run typecheck`, `npm test`

### Completion Notes List
- Nova rota `/sdr` registrada no App
- Novo item SDR adicionado no sidebar
- Tela SDR com fila do dia, quick actions, filtros e contador operacional
- Testes adicionados para renderizacao e filtro de cadencia

### File List
- packages/dashboard/src/App.jsx
- packages/dashboard/src/components/Layout.jsx
- packages/dashboard/src/components/Icon.jsx
- packages/dashboard/src/pages/SDR.jsx
- packages/dashboard/src/pages/dashboard.css
- packages/dashboard/src/components/__tests__/SDRPage.test.jsx

## QA Results
### Gate Executor
@qa + @architect

### Evidence (2026-03-02)
- `npm run test:regression:sdr-funil` -> PASS (`5 files / 12 tests`)
- `npm run lint` (dashboard) -> PASS
- `npm run typecheck` (dashboard) -> PASS

### Notes
- Aba SDR está operacional com fila, quick actions, filtros e métricas.
- Incrementos das waves 27/28 adicionaram filtro por vendedor e quick-assign sem regressão.

### QA Verdict
`GO` para fechamento da Story 24.2.

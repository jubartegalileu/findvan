---
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, performance_check]
---

# Story 24.3 — Aba Funil de Vendas: Pipeline Visual (Kanban)

**Epic:** 24 (SDR & Funil de Vendas: Workflow Linear)
**Story ID:** 24.3
**Status:** Ready
**Priority:** P0
**Complexity:** Alta (8 pontos)
**Blocked by:** Story 24.1

## Story

**As a** gestor/dono do negócio de transporte escolar,
**I want** a visual funnel tab with kanban board, drag-and-drop, and conversion metrics per stage,
**so that** I can see the health of my pipeline and identify bottlenecks at a glance.

## Acceptance Criteria

1. Navigation
- [ ] Aba "Funil" visível no sidebar (Layout.jsx) entre "SDR" e "WhatsApp"
- [ ] Rota `/funil` registrada no App.jsx
- [ ] Ícone consistente com o design system existente

2. Kanban Board
- [ ] 6 colunas correspondendo aos funnel_status: Novo, Contactado, Respondeu, Interessado, Convertido, Perdido
- [ ] Cada coluna mostra header com nome do stage e contagem de cards
- [ ] Cards exibem: nome, empresa (company_name), score badge, dias no stage atual (calculado de `entered_stage_at`)
- [ ] Coluna "Perdido" exibe `loss_reason` no card

3. Drag-and-Drop
- [ ] Implementado com @dnd-kit/core (adicionar dependência ao package.json)
- [ ] Mover lead entre stages via drag-and-drop
- [ ] Validação de transições: só permite moves válidos conforme `funnelTransitions` map
- [ ] Move inválido: card volta à posição original com feedback visual
- [ ] Ao mover para "Perdido": modal solicita `loss_reason` (obrigatório) antes de confirmar
- [ ] Cada move persiste via `PATCH /api/pipeline/:leadId/move`

4. Métricas por Stage
- [ ] Abaixo do header de cada coluna: quantidade, % do total
- [ ] Taxa de conversão para próximo stage exibida entre colunas (seta com %)
- [ ] Dados consumidos de `GET /api/pipeline/summary`

5. Barra de Resumo
- [ ] Barra fixa no topo com: total no pipeline, taxa de conversão geral (novo→convertido), tempo médio total
- [ ] Filtro por período: botões 7d / 30d / 90d / Tudo
- [ ] Filtro aplica em todas as métricas e cards exibidos

6. Performance
- [ ] Renderiza suavemente com até 500 leads distribuídos nos stages
- [ ] Drag-and-drop responsivo sem lag perceptível

7. Quality
- [ ] Testes unitários para componentes (KanbanColumn, FunnelCard, MetricsBar)
- [ ] `npm run lint` passa
- [ ] `npm run typecheck` passa

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend
**Secondary Type(s)**: API Integration, UX/Interaction Design
**Complexity**: Alta

### Specialized Agent Assignment

**Primary Agents**:
- @dev: Kanban implementation, drag-and-drop, API integration

**Supporting Agents**:
- @architect: Pattern validation, performance review

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Component structure, drag-and-drop logic, performance review
- [ ] Pre-PR (@architect): Performance validation (500 cards), UX consistency, transition validation

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: [CRITICAL, HIGH]

### CodeRabbit Focus Areas

**Primary Focus**:
- Drag-and-drop: Proper @dnd-kit implementation, accessibility (keyboard drag)
- Transition validation: Client-side check before API call, rollback on failure
- Performance: Virtualization if >100 cards per column

**Secondary Focus**:
- Loss reason modal: Proper form validation, required field enforcement
- Metrics calculation: Verify accuracy of conversion rates
- Error handling: Graceful fallback if pipeline API fails

## Tasks / Subtasks

- [ ] **Task 1: Setup e Dependências** (AC: 1)
  - [ ] Instalar `@dnd-kit/core` e `@dnd-kit/sortable`: `npm install @dnd-kit/core @dnd-kit/sortable`
  - [ ] Criar `packages/dashboard/src/pages/Funnel.jsx`
  - [ ] Adicionar rota `/funil` no App.jsx
  - [ ] Adicionar item "Funil" no sidebar em Layout.jsx (entre SDR e WhatsApp)

- [ ] **Task 2: Kanban Board** (AC: 2)
  - [ ] Criar componente `KanbanBoard` com layout horizontal de 6 colunas
  - [ ] Criar componente `KanbanColumn` com header (nome + count) e lista de cards
  - [ ] Criar componente `FunnelCard` com: nome, empresa, score (reusar ScoreBadge), dias no stage
  - [ ] Fetch de `GET /api/pipeline` para popular o board
  - [ ] Calcular "dias no stage" a partir de `entered_stage_at`

- [ ] **Task 3: Drag-and-Drop** (AC: 3)
  - [ ] Implementar DndContext com @dnd-kit/core wrapping o KanbanBoard
  - [ ] Implementar `onDragEnd` handler com validação de `funnelTransitions`
  - [ ] Se move inválido: retornar card à posição original + toast de erro
  - [ ] Se move para "Perdido": abrir modal de `loss_reason` antes de confirmar
  - [ ] Criar componente `LossReasonModal` com select de razões + campo "outro" (text)
  - [ ] Persistir move via `PATCH /api/pipeline/:leadId/move` com body: `{ to_status, loss_reason?, loss_reason_detail?, moved_by }`
  - [ ] Em caso de erro da API: reverter card à posição original + toast de erro

- [ ] **Task 4: Métricas** (AC: 4, 5)
  - [ ] Criar componente `FunnelMetricsBar` (barra fixa no topo)
  - [ ] Fetch de `GET /api/pipeline/summary` para métricas
  - [ ] Exibir por coluna: count, % do total
  - [ ] Exibir entre colunas: taxa de conversão (seta com %)
  - [ ] Barra de resumo: total pipeline, conversão geral, tempo médio
  - [ ] Implementar filtro por período: 7d / 30d / 90d / Tudo (query param `?period=7|30|90`)

- [ ] **Task 5: Performance** (AC: 6)
  - [ ] Testar com dataset de 500 leads mock
  - [ ] Se lag perceptível em colunas com >100 cards: implementar virtualização (react-window)
  - [ ] Otimizar re-renders: memoizar cards, usar React.memo em FunnelCard

- [ ] **Task 6: Tests** (AC: 7)
  - [ ] Testes unitários para FunnelCard, KanbanColumn
  - [ ] Testes para validação de funnelTransitions
  - [ ] Testes para LossReasonModal
  - [ ] Verificar lint e typecheck

## Dev Notes

### Funnel Transitions Map (validar client-side E server-side)

```javascript
const funnelTransitions = {
  novo: ['contactado', 'perdido'],
  contactado: ['respondeu', 'perdido'],
  respondeu: ['interessado', 'perdido'],
  interessado: ['convertido', 'perdido'],
  convertido: ['perdido'],
  perdido: ['novo']
};
```

### Loss Reason Options (de Leads.jsx)

```javascript
const lossReasonOptions = [
  { value: 'sem_interesse', label: 'Sem interesse' },
  { value: 'ja_tem_fornecedor', label: 'Já tem fornecedor' },
  { value: 'preco_alto', label: 'Preço alto' },
  { value: 'sem_resposta_3_tentativas', label: 'Sem resposta (3 tentativas)' },
  { value: 'numero_invalido_ou_bloqueado', label: 'Número inválido/bloqueado' },
  { value: 'outro', label: 'Outro' }
];
```

Quando "outro" selecionado, exibir campo text para `loss_reason_detail`.

### @dnd-kit Quick Reference

```jsx
import { DndContext, closestCorners } from '@dnd-kit/core';
import { SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable';
import { useSortable } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';

// Wrapper
<DndContext collisionDetection={closestCorners} onDragEnd={handleDragEnd}>
  {columns.map(col => (
    <SortableContext items={col.items} strategy={verticalListSortingStrategy}>
      <KanbanColumn ... />
    </SortableContext>
  ))}
</DndContext>
```

### Relevant Source Tree

```
packages/dashboard/src/
├── App.jsx                    # Adicionar rota /funil
├── components/
│   ├── Layout.jsx             # Adicionar item Funil no sidebar
│   ├── ScoreBadge.jsx         # REUSAR nos cards
│   ├── KanbanBoard.jsx        # CRIAR
│   ├── KanbanColumn.jsx       # CRIAR
│   ├── FunnelCard.jsx         # CRIAR
│   ├── FunnelMetricsBar.jsx   # CRIAR
│   └── LossReasonModal.jsx    # CRIAR
├── pages/
│   ├── Funnel.jsx             # CRIAR
│   └── SDR.jsx                # Criada na Story 24.2
```

### API Endpoints (de Story 24.1)

```
GET    /api/pipeline              → { stages: { novo: [...], contactado: [...], ... } }
GET    /api/pipeline/summary      → { stages: [{ status, count, pct, conversion_rate, avg_days }], total, overall_conversion }
PATCH  /api/pipeline/:leadId/move → body: { to_status, loss_reason?, loss_reason_detail?, moved_by }
GET    /api/pipeline/history      → [{ lead_id, from, to, moved_by, moved_at }]
```

### Testing

- **Foco:** Validação de transitions, renderização do board, modal de loss_reason
- **Mock:** Criar fixture com 500 leads distribuídos nos 6 stages para teste de performance

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-01 | 1.0 | Story criada a partir do Epic 24 | @sm (River) |

## Dev Agent Record

### Agent Model Used
GPT-5 Codex (CLI)

### Debug Log References
- Implementacao da aba Funil em packages/dashboard/src/pages/Funnel.jsx
- Migracao para DnD via -kit/core
- Validacao dashboard com lint, typecheck e vitest

### Completion Notes List
- Nova rota /funil registrada no App
- Novo item Funil adicionado no sidebar
- Dependencia corrigida: electron-squirrel-startup para versao existente
- Dependencia adicionada: -kit/core
- Board visual por estagio com DndContext (-kit/core)
- Modal de motivo de perda para transicao para Perdido
- Integracao com /api/pipeline e /api/pipeline/summary
- Testes adicionados para renderizacao e filtro de periodo

### File List
- packages/dashboard/package.json
- packages/dashboard/package-lock.json
- packages/dashboard/src/App.jsx
- packages/dashboard/src/components/Layout.jsx
- packages/dashboard/src/pages/Funnel.jsx
- packages/dashboard/src/pages/dashboard.css
- packages/dashboard/src/components/__tests__/FunnelPage.test.jsx

## QA Results
_To be filled by QA agent_

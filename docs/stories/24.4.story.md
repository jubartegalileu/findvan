---
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [regression_test, code_review, integration_validation]
---

# Story 24.4 — Refatorar Leads.jsx + Dashboard com Endpoints Reais

**Epic:** 24 (SDR & Funil de Vendas: Workflow Linear)
**Story ID:** 24.4
**Status:** Done
**Priority:** P0
**Owner:** @dev
**Estimate:** TBD
**Complexity:** Média (5 pontos)
**Blocked by:** Story 24.1, 24.2, 24.3

## Story

**As a** FindVan user,
**I want** the Leads tab to show only contact data and the Dashboard to consume real backend data,
**so that** there's no duplicate logic across tabs and all data is consistent from the database.

## Acceptance Criteria

1. Leads.jsx Cleanup
- [x] Removida toda lógica de `funnel_status` (variáveis, filtros, display, funnelTransitions map)
- [x] Removida toda lógica de `prospect_status` (variáveis, filtros, display)
- [x] Removida toda lógica de `next_action_date`, `next_action_description` (cadência, SLA, follow-up)
- [x] Removida toda lógica de `prospect_notes`
- [x] Removida toda lógica de `loss_reason` e `loss_reason_other`
- [x] Removidas funções: `isFollowUpOverdue()`, `isFollowUpToday()`, `isFollowUpPlanned()`, `getSlaMeta()`, `getCadenceStatus()`
- [x] Removidos componentes/seções de UI que exibiam funil, cadência, SLA
- [x] Leads.jsx consome `GET /api/leads` para listar leads (dados cadastrais apenas)
- [x] Score e tags continuam funcionando na aba Leads
- [x] Filtros restantes funcionam: por cidade, por source, por is_valid, por busca textual

2. Dashboard.jsx Refactor
- [x] Dashboard consome `GET /api/pipeline/summary` para funnel summary (em vez de endpoint inexistente)
- [x] Dashboard consome `GET /api/sdr/stats` para KPIs de atividade SDR
- [x] KPIs refletem dados reais persistidos no banco
- [x] Funnel summary no Dashboard exibe dados reais de pipeline
- [x] Seções que dependiam de dados em memória agora consomem APIs reais
- [x] Fallback graceful se API retorna erro (exibir "dados indisponíveis" em vez de crash)

3. Regression
- [x] Aba Leads renderiza corretamente com dados de `GET /api/leads`
- [x] Aba Dashboard renderiza corretamente com dados reais
- [x] Abas Scraper, WhatsApp, Campanhas, Configurações não foram afetadas
- [x] Nenhuma funcionalidade foi perdida — tudo que foi removido existe agora em SDR ou Funil
- [x] Navegação entre todas as 8 abas funciona sem erros

4. Quality
- [x] Testes de regressão para Leads.jsx (renderização, filtros restantes)
- [x] Testes de regressão para Dashboard.jsx (KPIs, funnel summary)
- [x] `npm run lint` passa
- [x] `npm run typecheck` passa

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend Refactoring
**Secondary Type(s)**: API Integration
**Complexity**: Média

### Specialized Agent Assignment

**Primary Agents**:
- @dev: Refactoring implementation
- @qa: Regression validation

**Supporting Agents**:
- @architect: Pattern consistency review

### Quality Gate Tasks

- [x] Pre-Commit (@dev): Regression check on Leads and Dashboard
- [x] Pre-PR (@qa): Full integration validation, verify no orphaned code

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: [CRITICAL, HIGH]

### CodeRabbit Focus Areas

**Primary Focus**:
- Dead code removal: Ensure ALL funnel/cadence/SLA logic removed from Leads.jsx
- API migration: All data consumption moved from in-memory defaults to real API calls
- Regression safety: No functionality lost, just relocated

**Secondary Focus**:
- Error handling: Dashboard graceful fallback if new APIs aren't responding
- Consistency: Same API_BASE pattern used across all new fetch calls

## Tasks / Subtasks

- [x] **Task 1: Audit Leads.jsx** (AC: 1)
  - [x] Catalogar todas as referências a: `funnel_status`, `prospect_status`, `next_action_date`, `next_action_description`, `prospect_notes`, `loss_reason`, `loss_reason_other`
  - [x] Catalogar todas as funções de cadência/SLA: `isFollowUpOverdue`, `isFollowUpToday`, `isFollowUpPlanned`, `getSlaMeta`, `getCadenceStatus`
  - [x] Catalogar componentes/seções de UI afetadas (filtros de funil, badges de cadência, etc.)

- [x] **Task 2: Refatorar Leads.jsx** (AC: 1)
  - [x] Substituir fetch atual por `GET /api/leads` (com paginação)
  - [x] Remover todas as variáveis e constantes de funil/cadência
  - [x] Remover todas as funções de cadência/SLA
  - [x] Remover seções de UI de funil, cadência, prospect_notes
  - [x] Manter: filtros por cidade, source, is_valid, busca textual
  - [x] Manter: ScoreBadge, tags, dados cadastrais
  - [x] Manter: modal de edição de lead (apenas dados cadastrais)
  - [x] Verificar que o mapeamento de dados do fetch não espera campos removidos

- [x] **Task 3: Refatorar Dashboard.jsx** (AC: 2)
  - [x] Identificar todos os fetches a endpoints inexistentes (`/api/dashboard/kpis`, `/api/dashboard/funnel-summary`)
  - [x] Substituir `/api/dashboard/funnel-summary` por `GET /api/pipeline/summary`
  - [x] Substituir KPIs de contatos/atividade por `GET /api/sdr/stats`
  - [x] Manter fetches existentes que funcionam (scraper runs, activity, etc.)
  - [x] Adicionar fallback graceful: se endpoint retorna erro, exibir placeholder "Dados indisponíveis"
  - [x] Remover referências a dados de funil calculados em memória

- [x] **Task 4: Regression Testing** (AC: 3)
  - [x] Verificar manualmente navegação entre todas as 8 abas
  - [x] Verificar que Leads exibe dados corretamente sem campos de funil
  - [x] Verificar que Dashboard exibe KPIs e funnel summary de APIs reais
  - [x] Verificar que Scraper, WhatsApp, Campanhas, Configurações não foram afetadas
  - [x] Criar checklist de funcionalidades que devem existir em SDR (de 24.2) e Funil (de 24.3)

- [x] **Task 5: Cleanup e Tests** (AC: 4)
  - [x] Remover imports não utilizados
  - [x] Remover constantes orphaned (funnelStatusOptions, funnelTransitions, lossReasonOptions, prospectStatusOptions)
  - [x] Testes de regressão para Leads.jsx
  - [x] Testes de regressão para Dashboard.jsx
  - [x] Verificar lint e typecheck

## Dev Notes

### Campos a Remover de Leads.jsx

Referência completa dos campos que hoje existem apenas em memória no frontend e devem ser removidos:

```javascript
// Estes defaults são aplicados no mapeamento do fetch — REMOVER TODOS
prospect_status: lead.prospect_status || 'nao_contatado',
prospect_notes: lead.prospect_notes || '',
next_action_date: lead.next_action_date || null,
next_action_description: lead.next_action_description || '',
funnel_status: lead.funnel_status || 'novo',
loss_reason: (lead.loss_reason || '').startsWith('outro:') ? 'outro' : lead.loss_reason || '',
loss_reason_other: (lead.loss_reason || '').startsWith('outro:') ? (lead.loss_reason || '').slice(6) : '',
```

### Constantes a Remover de Leads.jsx

```javascript
// REMOVER — agora vivem na aba SDR e Funil
funnelStatusOptions
funnelTransitions
lossReasonOptions
prospectStatusOptions
```

### Dashboard Endpoints — Mapeamento

| Fetch Atual (inexistente) | Substituir Por | Fonte |
|---------------------------|---------------|-------|
| `/api/dashboard/kpis` | `/api/sdr/stats` + `/api/pipeline/summary` | Story 24.1 |
| `/api/dashboard/funnel-summary` | `/api/pipeline/summary` | Story 24.1 |

**Manter** (já funcionam ou são de outros módulos):
- `/api/scraper/runs`
- `/api/activity`
- `/api/integrations/messaging/receipts`
- `/api/dashboard/metrics-governance` (se existir)

### Relevant Source Tree

```
packages/dashboard/src/
├── pages/
│   ├── Leads.jsx              # REFATORAR (remover lógica de funil/cadência)
│   ├── Dashboard.jsx          # REFATORAR (consumir APIs reais)
│   ├── SDR.jsx                # Já criada (Story 24.2) — contém lógica de cadência
│   └── Funnel.jsx             # Já criada (Story 24.3) — contém lógica de funil
```

### Checklist de Funcionalidades Relocadas

| Funcionalidade | Origem (Leads.jsx) | Destino |
|---------------|---------------------|---------|
| Filtro por funnel_status | Leads.jsx | Funil.jsx (kanban columns) |
| Filtro por cadência (overdue/today) | Leads.jsx | SDR.jsx (filtro de fila) |
| Edição de funnel_status | Leads.jsx | Funil.jsx (drag-and-drop) |
| Edição de next_action_date | Leads.jsx | SDR.jsx (quick action "feito") |
| Prospect notes | Leads.jsx | SDR.jsx (quick action "anotar") |
| Loss reason | Leads.jsx | Funil.jsx (LossReasonModal) |
| SLA/Cadence badges | Leads.jsx | SDR.jsx (card indicators) |
| Funnel summary KPIs | Dashboard.jsx (in-memory) | Dashboard.jsx → `/api/pipeline/summary` |

### Testing

- **Foco:** Regressão — garantir que nada quebrou
- **Approach:** Antes de remover qualquer código, verificar que a funcionalidade equivalente existe em SDR.jsx ou Funnel.jsx
- **Risk:** Esta é a story de maior risco de regressão — testar extensivamente

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-01 | 1.0 | Story criada a partir do Epic 24 | @sm (River) |

## Dev Agent Record

### Agent Model Used
Codex (GPT-5)

### Debug Log References
- Dashboard endpoint migration:
  - `/api/dashboard/kpis` -> `/api/sdr/stats`
  - `/api/dashboard/funnel-summary` -> `/api/pipeline/summary`
- Regression tests:
  - `dashboard/src/components/__tests__/DashboardPage.test.jsx`
  - `dashboard/src/components/__tests__/LeadsPage.test.jsx`
  - `dashboard/src/components/__tests__/NavigationSmoke.test.jsx`
- Validation commands:
  - `npm run lint` (dashboard) -> PASS
  - `npm run typecheck` (dashboard) -> PASS
  - `npm test -- --run LeadsPage.test.jsx DashboardPage.test.jsx FunnelPage.test.jsx SDRPage.test.jsx` -> PASS (8 tests)
  - `npm test -- --run NavigationSmoke.test.jsx LeadsPage.test.jsx DashboardPage.test.jsx FunnelPage.test.jsx SDRPage.test.jsx` -> PASS (10 tests)

### Completion Notes List
- Dashboard refatorado para usar endpoints reais de SDR/Pipeline com fallback visual de "Dados indisponíveis".
- Mini-funil adaptado para payload real de `/api/pipeline/summary` (`pct`, `overall_conversion`).
- Navegação do mini-funil e ações urgentes alinhada para abas novas (`/funil` e `/sdr`).
- Testes de regressão adicionados para Dashboard cobrindo integração e fallback.
- `Leads.jsx` reescrito para escopo estrito da story: dados cadastrais + score/tags + filtros básicos + paginação.
- Payload de `PATCH /api/leads/{id}` higienizado para não enviar campos de funil/cadência/prospect.
- Smoke de navegação automatizado validando as 8 rotas/tabs e fallback de rota inexistente.

### File List
- `packages/dashboard/src/pages/Dashboard.jsx`
- `packages/dashboard/src/components/__tests__/DashboardPage.test.jsx`
- `packages/dashboard/src/pages/Leads.jsx`
- `packages/dashboard/src/components/__tests__/LeadsPage.test.jsx`
- `packages/dashboard/src/components/__tests__/NavigationSmoke.test.jsx`

## QA Results
### QA Gate — 2026-03-01

**Verdict:** GO

**Executed checks**
- Dashboard:
  - `npm run lint` -> PASS
  - `npm run typecheck` -> PASS
  - `npm test -- --run NavigationSmoke.test.jsx LeadsPage.test.jsx DashboardPage.test.jsx FunnelPage.test.jsx SDRPage.test.jsx` -> PASS (10 tests)
- Backend API regression:
  - `./venv/bin/python -m pytest -q tests/api/test_leads_api.py tests/api/test_dashboard_api.py tests/api/test_sdr_api.py tests/api/test_pipeline_api.py` -> PASS (49 tests)

**Notes**
- Warnings `act(...)` in SDR/Funnel tests remain as non-blocking test hygiene issue (existing behavior), no functional regression observed.
- Story acceptance criteria for Leads cleanup, Dashboard real endpoints, and navigation smoke are satisfied.

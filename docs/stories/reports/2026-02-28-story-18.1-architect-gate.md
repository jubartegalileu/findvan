# Architect Gate — Story 18.1 (Score & Funnel Data Consistency)

**Date:** 2026-02-28  
**Agent:** @architect  
**Status:** PASS

## Scope Reviewed

- Normalização idempotente de score + funnel status para base de leads.
- Garantia de consistência em eventos de importação e edição.
- Contrato aditivo de API para execução/auditoria da normalização.

## Architectural Validation

1. Consistency model
- `normalize_funnel_status` padroniza valores para conjunto permitido com fallback seguro em `novo`.
- `insert_leads` e `update_lead` passaram a aplicar normalização consistente de status.
- Recalculo de score segue fórmula determinística existente (`score_service`) sem duplicidade de regra.

2. Migration safety
- `normalize_leads_consistency` executa varredura controlada com saída auditável:
  - `scanned`, `updated`, `score_updated`, `status_normalized`, `loss_reason_cleared`.
- Processo é idempotente: nova execução sem mudanças adicionais tende a `updated = 0`.
- Não há remoção de endpoint/campo legado; mudança é compatível.

3. API contract and compatibility
- Endpoint aditivo `POST /api/leads/normalize-consistency` introduzido sem quebrar rotas existentes.
- Contratos atuais de Leads, Dashboard e batch permanecem funcionais.

4. Validation evidence
- Testes de API atualizados (`test_leads_api.py`, `test_leads_batch_api.py`).
- Quality gates verdes no ciclo: `lint`, `typecheck`, `test`.

## Residual Risks (non-blocking)

- Recomendável execução controlada da normalização em janela operacional para bases muito grandes, com observação de tempo de execução.

## Verdict

- Story 18.1 aprovada no gate @architect.
- Sem bloqueio arquitetural para avanço ao gate @qa.
